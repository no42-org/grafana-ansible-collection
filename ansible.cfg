[defaults]
# Singular. The plural `collections_paths` was an alias that ansible-core 2.18
# deprecates and 2.19 removed; on 2.21 the plural is silently ignored and the
# path falls back to ~/.ansible/collections. The singular is accepted across
# the whole range requires_ansible claims.
collections_path = ./
